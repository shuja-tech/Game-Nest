# Admin & User Panels

Rollback note:
- Admin backend endpoints were reverted/removed from `main.py`.
- User/Admin panel pages/templates/static may remain in repo but are no longer reachable via backend routes.

